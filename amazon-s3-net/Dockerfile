FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
COPY . /source
WORKDIR /source/
RUN dotnet publish -c release -o /app

FROM mcr.microsoft.com/dotnet/aspnet:10.0
WORKDIR /app
COPY --from=build /app ./
ENTRYPOINT ["dotnet", "amazon-s3-net.dll", "--urls=http://*"]
